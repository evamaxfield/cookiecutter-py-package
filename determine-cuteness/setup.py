from setuptools import find_packages, setup

setup(
    author="Eva",
    description="A single function to determine the cuteness of various animals.",
    name="determine_cuteness",
    packages=find_packages(),
    version="0.0.0",
)
